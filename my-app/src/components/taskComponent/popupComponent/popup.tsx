import { useState } from 'react';
import ITask from "../../../interfaces/Task"

/* interface IProps{
    name: string,
    description: string
}

const Popup: React.FC<IProps> = ({name, description}) => {

    const [taskList, setTaskList] = useState<ITask[]>([]);

    return (<div>
        <input onClick={() => ({})} type="button" value="update"></input>
    
    </div>)
}

export default Popup; */